name: CI/CD Pipeline

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Login to Docker Hub
      run: |
        echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

    - name: Build Docker image
      run: |
        docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/url-shortener:latest .
        echo "✅ URL Shortener built successfully!"

    - name: Push to Docker Hub
      run: |
        docker push ${{ secrets.DOCKERHUB_USERNAME }}/url-shortener:latest
        echo "✅ Image pushed to Docker Hub!"

    - name: Notify Telegram - SUCCESS
      if: success()
      uses: appleboy/telegram-action@master
      with:
        to: ${{ secrets.TELEGRAM_CHAT_ID }}
        token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        message: |
          🎉 URL SHORTENER DEPLOYED!
          
          📦 Image: ${{ secrets.DOCKERHUB_USERNAME }}/url-shortener:latest
          🚀 Features:
          • Shorten URLs
          • Visit statistics  
          • REST API
          
          🔗 Test it:
          docker run -p 8000:8000 ${{ secrets.DOCKERHUB_USERNAME }}/url-shortener
          
          👤 By: ${{ github.actor }}
